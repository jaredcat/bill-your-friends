import * as React from 'react';
import ListItem from './ListItem';

type Props = {};

const List = ({}: Props) => (
  <ul>
    {/* {items.map((item) => (
      <li key={item.id}>
        <ListItem data={item} />
      </li>
    ))} */}
  </ul>
);

export default List;
